spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

seatlock:
  jwt:
    secret: "seatlock-test-jwt-secret-change-in-prod-32chars"
  cache:
    slots-ttl-seconds: 1

logging:
  level:
    com.seatlock: DEBUG
