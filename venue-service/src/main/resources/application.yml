server:
  port: 8082

spring:
  application:
    name: venue-service
  datasource:
    url: jdbc:postgresql://localhost:5432/venue_db
    username: seatlock
    password: seatlock
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health
